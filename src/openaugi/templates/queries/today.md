---
kind: engine
description: Everything with today's content date.
query:
  after: "today"
---

Today's blocks by content date (block_time). `today` resolves at run
time, so this file never goes stale.
